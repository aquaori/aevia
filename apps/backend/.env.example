# JWT secret is required in production. Use a long random string.
JWT_SECRET=replace_with_a_long_random_secret
SESSION_TOKEN_TTL=30m
INVITE_TOKEN_TTL=7d
WS_HEARTBEAT_INTERVAL_MS=30000
# if you have own sqlite path, use `DB_PATH` to config
# DB_PATH=your_own_db_path
